# MongoDB Configuration (Local)
# Default: Uses local MongoDB container with authentication
# Override to use MongoDB Atlas or other remote instance
# MONGODB_URI=mongodb://admin:admin123@mongodb:27017/coordinator_db?authSource=admin
# MONGODB_DATABASE=coordinator_db

# MongoDB Configuration (Remote - Atlas)
# Uncomment to use MongoDB Atlas instead of local container
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
# MONGODB_DATABASE=coordinator_db_prod

# Qdrant Configuration (Local)
# Default: Uses local Qdrant container
# Override to use Qdrant Cloud or other remote instance
# QDRANT_URL=http://qdrant:6333

# Qdrant Configuration (Remote - Qdrant Cloud)
# Uncomment to use Qdrant Cloud instead of local container
# QDRANT_URL=https://your-cluster-id.region.gcp.cloud.qdrant.io:6333
# QDRANT_API_KEY=your-qdrant-api-key-here

# OpenAI API Configuration
# Required for semantic embeddings (text-embedding-3-small)
# Falls back to mock embeddings if not provided
OPENAI_API_KEY=sk-your-openai-api-key-here

# Logging
# LOG_LEVEL=info
